{% extends 'base_fr.html.twig' %}

{% block title %}BABDay 2022 - FAQ{% endblock %}

{% block body %}
    <body class ="bgImage" style="background-image:url({{ asset('image/lines2.png') }});">
      <div class="container pt-50 pb-50">
        <div class="accordion accordion-flush" id="accordionFlushExample">

          <!--
          affichage des FAQ
          -->

          {% for faq in faq_fr %}
          <div class="accordion-item">
            <h2 class="accordion-header" id="{{ 'flush-heading'~faq.id }}">
              <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#{{ 'flush-collapse'~faq.id }}" aria-expanded="false" aria-controls="{{ 'flush-collapse'~faq.id }}">
                {{ faq.question }}
              </button>
            </h2>
            <div id="{{ 'flush-collapse'~faq.id }}" class="accordion-collapse collapse " aria-labelledby="{{ 'flush-heading'~faq.id }}" data-bs-parent="#accordionFlushExample">
              <div class="accordion-body  ">
                {{ faq.answer }}
              </div>
            </div>
          </div>
            {% if is_granted('ROLE_ADMIN') %}
              <p>
                <a href="{{ path('updateFaq', {id:faq.id}) }}" class="btn btn-outline-warning">Update</a>
              </p>
            {%  endif %}
          {% endfor %}
        </div>
        <div class="faq-form pt-50">

            <p class="otherQ fw-700"> Votre question n'apparait pas ci-dessus ? envoyez un e-mail à : <a href="mailto:info@babday.be">info@babday.be</a></p></p>
            <!--
            <form action="{{ path('contact') }}" method="post">
                <div class="form-group">
                    <label for="eMail">Votre e-mail</label>
                    <input type="email" class="form-control" id="eMail" aria-describedby="emailHelp" placeholder="exemple : john.doe@email.com" name="eMail" required="champs obligatoire">
                </div>
                <div class="form-group">
                    <label for="question">Votre question</label>
                    <textarea class="form-control" id="question" rows="3" placeholder="exemple : Quel temps fera t'il le 6 avril ?" name="question" required="champs obligatoire"></textarea>
                </div>
                <div class="row">
                    <div class="col-6">
                        <div class="form-group">
                            <label for="famillyName">votre nom de famille</label>
                            <input type="text" class="form-control" id="famillyName" placeholder="exemple : doe" name="lastname" required="champs obligatoire">
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="form-group">
                            <label for="firstname">Votre prénom</label>
                            <input type="text" class="form-control" id="firstname" placeholder="exemple : John" name="firstname" required="champs obligatoire">
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn  btn-primary btn-faq">Envoyer</button>
            </form>
            -->
      </div>

      </div>
    </body>
    {%  include 'footer/footer_fr.html.twig' %}
{% endblock body %}